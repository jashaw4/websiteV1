# websiteV1

Personal website rebuilt in SvelteKit.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The site is configured for static output, so it can be deployed like the original version.
